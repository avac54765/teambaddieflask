version: '3'
services:
        web:
                image: flask_teambaddies_v1
                build: .
                ports:
                        - "6324:8086"
                volumes:
                        - ./volumes:/volumes
                        - ./instance:/instance
                restart: unless-stopped
