version: '3'
services:
        web:
                image: flask_teambaddies_v1
                build: .
                ports:
                        - "6555:6555"
                volumes:
                        - ./volumes:/volumes
                        - ./instance:/instance
                restart: unless-stopped
